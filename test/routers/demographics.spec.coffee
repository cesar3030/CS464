# global beforeEach, describe, it, assert, expect
"use strict"

describe 'Demographics Router', ->
  beforeEach ->
    @DemographicsRouter = new Cs464.Routers.Demographics();

  it 'index route', ->

